# Demo evidence

Drop the live-demo screenshots here (these are the recorded evidence for
feat-005 / feat-006):

- `before.png` — a product's price in Shopify admin before applying a rule.
- `after.png` — the same product after Apply: new price + strikethrough
  compare-at "sale" price.
- `app.png` — the BulkPrice app screen with the preview table + Apply button.

Capture them by running the live demo (see `docs/PRESENTATION.md` → Live demo
script): `npm run config:link` then `npm run dev` against a Partner dev store.
